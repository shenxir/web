<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.lostfound.mapper.CategoryMapper">

    <resultMap id="CategoryResultMap" type="org.example.lostfound.model.Category">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="icon" column="icon"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="status" column="status"/>
    </resultMap>

    <!-- 查询所有启用的分类 -->
    <select id="getAllCategories" resultMap="CategoryResultMap">
        SELECT id, name, icon, sort_order, status
        FROM sys_category
        WHERE status = 1
        ORDER BY sort_order ASC
    </select>

    <!-- 根据ID查询分类 -->
    <select id="getCategoryById" resultMap="CategoryResultMap">
        SELECT id, name, icon, sort_order, status
        FROM sys_category
        WHERE id = #{categoryId}
    </select>

    <!-- 根据名称查询分类 -->
    <select id="getCategoryByName" resultMap="CategoryResultMap">
        SELECT id, name, icon, sort_order, status
        FROM sys_category
        WHERE name = #{name}
    </select>

    <!-- 新增分类 -->
    <insert id="insertCategory" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_category (name, icon, sort_order)
        VALUES (#{name}, #{icon}, #{sortOrder})
    </insert>

    <!-- 更新分类 -->
    <update id="updateCategory">
        UPDATE sys_category
        SET name = #{name}, icon = #{icon}, sort_order = #{sortOrder}
        WHERE id = #{id}
    </update>

    <!-- 删除分类 -->
    <update id="deleteCategory">
        UPDATE sys_category SET status = 0 WHERE id = #{categoryId}
    </update>

</mapper>
